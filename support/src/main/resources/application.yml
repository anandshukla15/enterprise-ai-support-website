spring:

  datasource:
    url: jdbc:mysql://localhost:3306/aidesk?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: anandshukla$15

  jpa:
    hibernate:
      ddl-auto: validate

    show-sql: true

    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true



server:
  port: 8080


logging:
  level:
    org.hibernate.SQL: debug
